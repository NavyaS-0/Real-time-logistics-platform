services:

  postgres:
    image: postgres:17
    container_name: logistics-postgres
    restart: unless-stopped

    environment:
      POSTGRES_DB: logistics
      POSTGRES_USER: logistics_user
      POSTGRES_PASSWORD: ${DB_PASSWORD:-logistics_pass}

    ports:
      - "5432:5432"

    volumes:
      - postgres_data:/var/lib/postgresql/data


  redis:
    image: redis:7-alpine
    container_name: logistics-redis
    restart: unless-stopped

    ports:
      - "6379:6379"

    volumes:
      - redis_data:/data


  kafka:
    image: apache/kafka:4.0.0
    container_name: logistics-kafka
    restart: unless-stopped

    ports:
      - "9092:9092"

    environment:
      KAFKA_NODE_ID: 1
      KAFKA_PROCESS_ROLES: broker,controller

      KAFKA_LISTENERS: PLAINTEXT://:9092,CONTROLLER://:9093
      KAFKA_ADVERTISED_LISTENERS: PLAINTEXT://kafka:9092

      KAFKA_CONTROLLER_LISTENER_NAMES: CONTROLLER
      KAFKA_CONTROLLER_QUORUM_VOTERS: 1@localhost:9093

      KAFKA_LISTENER_SECURITY_PROTOCOL_MAP: CONTROLLER:PLAINTEXT,PLAINTEXT:PLAINTEXT

      KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR: 1

      KAFKA_TRANSACTION_STATE_LOG_REPLICATION_FACTOR: 1
      KAFKA_TRANSACTION_STATE_LOG_MIN_ISR: 1

      KAFKA_GROUP_INITIAL_REBALANCE_DELAY_MS: 0

      KAFKA_NUM_PARTITIONS: 3


  shipment-service:
    image: logistics-shipment-service:latest
    container_name: logistics-shipment-service
    restart: unless-stopped

    ports:
      - "8081:8081"

    depends_on:
      - postgres
      - kafka

    environment:
      SPRING_DATASOURCE_URL: jdbc:postgresql://postgres:5432/logistics
      SPRING_DATASOURCE_USERNAME: logistics_user
      SPRING_DATASOURCE_PASSWORD: ${DB_PASSWORD:-logistics_pass}
      SPRING_KAFKA_BOOTSTRAP_SERVERS: kafka:9092


  tracking-service:
    image: logistics-tracking-service:latest
    container_name: logistics-tracking-service
    restart: unless-stopped

    ports:
      - "8082:8082"

    depends_on:
      - postgres
      - kafka
      - redis

    environment:
      SPRING_DATASOURCE_URL: jdbc:postgresql://postgres:5432/logistics?options=-c%20TimeZone%3DUTC
      SPRING_DATASOURCE_USERNAME: logistics_user
      SPRING_DATASOURCE_PASSWORD: ${DB_PASSWORD:-logistics_pass}

      SPRING_KAFKA_BOOTSTRAP_SERVERS: kafka:9092

      SPRING_DATA_REDIS_HOST: redis
      SPRING_DATA_REDIS_PORT: 6379


  eta-service:
    image: logistics-eta-service:latest
    container_name: logistics-eta-service
    restart: unless-stopped

    ports:
      - "8000:8000"

    depends_on:
      - kafka
      - redis

    environment:
      KAFKA_BOOTSTRAP_SERVERS: kafka:9092
      REDIS_HOST: redis
      REDIS_PORT: 6379


  alert-service:
    image: logistics-alert-service:latest
    container_name: logistics-alert-service
    restart: unless-stopped

    ports:
      - "8001:8001"

    depends_on:
      - kafka

    environment:
      KAFKA_BOOTSTRAP_SERVERS: kafka:9092


volumes:
  postgres_data:
  redis_data: